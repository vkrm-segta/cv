/** @type {import('next').NextConfig} */
const nextConfig = {
    // i18n: {
    //     defaultLocale: "en", //default locale
    //     locales: ["en", "hb"], // list of supported locales
    //     localeDetection: false
    // },
};

export default nextConfig;
